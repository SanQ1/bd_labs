add-ratings-table migration: Додаю нову таблицю рейтингу, для якої визначаю колонку rating, для зберігання рейтингу, та два foreign keys: customers, products.

add-orders-column: Додаю нову колонку до таблиці orders - is_ready, яка визначає чи готове замовлення.

remove-customers-column: Видаляю колонку surname для customer. Для ім'я тепер використовується одна колонка name.
